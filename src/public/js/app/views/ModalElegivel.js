// Generated by CoffeeScript 2.7.0
define(['backbone.modal'], function(ModalView) {
  var ModalElegivel;
  return ModalElegivel = (function() {
    class ModalElegivel extends Backbone.Modal {
      backToProgresso() {
        App.appRouter.navigate('#comp/progresso', {
          'trigger': true
        });
        return this.destroy();
      }

      onRender() {
        var b;
        b = $('body');
        if (!b.is(':visible')) {
          return b.fadeIn();
        }
      }

      onDestroy() {
        return $('body').css({
          'overflow': 'auto'
        });
      }

    };

    ModalElegivel.prototype.template = _.template($('#modal-elegivel-conteudo-item').html(), {
      interpolate: /\<\@\=(.+?)\@\>/gim,
      evaluate: /\<\@([\s\S]+?)\@\>/gim,
      escape: /\<\@\-(.+?)\@\>/gim
    });

    ModalElegivel.prototype.submitEl = '.bbm-button';

    ModalElegivel.prototype.events = {
      'click #link-meu-progresso': 'backToProgresso'
    };

    return ModalElegivel;

  }).call(this);
});

//   App.modals.destroyAll() if App.modals.currentView
