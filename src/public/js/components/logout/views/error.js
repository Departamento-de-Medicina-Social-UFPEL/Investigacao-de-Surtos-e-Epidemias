// Generated by CoffeeScript 2.7.0
define(['backbone.modal'], function() {
  var Modal;
  return Modal = (function() {
    class Modal extends Backbone.Modal {
      initialize(obj) {
        return this.model = new Backbone.Model(obj);
      }

      onRender() {
        var b;
        b = $('body');
        if (!b.is(':visible')) {
          return b.fadeIn();
        }
      }

      onDestroy() {
        $('body').css({
          'overflow': 'auto'
        });
        return window.location.hash = '';
      }

    };

    Modal.prototype.submitEl = '.bbm-button';

    Modal.prototype.template = _.template($('#logout-error').html());

    return Modal;

  }).call(this);
});
