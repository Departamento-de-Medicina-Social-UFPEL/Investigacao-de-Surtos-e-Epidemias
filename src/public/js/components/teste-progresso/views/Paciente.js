// Generated by CoffeeScript 2.7.0
var makeIdade;

define([], function() {
  var Paciente;
  Paciente = (function() {
    class Paciente extends Marionette.ItemView {
      initialize(options) {
        return console.log(this.model.get('dataLongaPub'));
      }

    };

    Paciente.prototype['tagName'] = 'div';

    Paciente.prototype['className'] = 'slide pacienteInfo container-fluid';

    Paciente.prototype['template'] = '#caso-progresso-paciente';

    Paciente.prototype['templateHelpers'] = {
      'strIdade': function() {
        return makeIdade(this.idade);
      }
    };

    return Paciente;

  }).call(this);
  return Paciente;
});

makeIdade = function(strIdade) {
  var i, nomes, num, plural, ret, singular, uni;
  if (typeof strIdade !== "string") {
    return 0;
  }
  console.log(strIdade, 'strIdade');
  num = strIdade.match(/\d{1,}/g);
  uni = strIdade.match(/[dams]/g);
  singular = {
    d: "dia",
    a: "ano",
    m: "mês",
    s: "semana"
  };
  plural = {
    d: "dias",
    a: "anos",
    m: "meses",
    s: "semanas"
  };
  ret = "";
  for (i in num) {
    nomes = (parseInt(num[i], 10) > 1 ? plural : singular);
    ret += (ret.length > 0 ? " e " : "") + num[i] + " " + nomes[uni[i]];
  }
  return ret;
};
