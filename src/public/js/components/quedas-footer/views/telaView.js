// Generated by CoffeeScript 2.7.0
define(['backbone.marionette'], function(Marionette) {
  var IntroItemView;
  IntroItemView = (function() {
    class IntroItemView extends Marionette.ItemView {
      onRender() {
        var changeLogo, self;
        self = this;
        this.ui.seg.hide();
        changeLogo = function() {
          if (self.ui.prim.is(':visible')) {
            return self.ui.prim.fadeOut(100, function() {
              if (self.ui.seg) {
                return self.ui.seg.fadeIn();
              }
            });
          } else {
            return self.ui.seg.fadeOut(100, function() {
              if (self.ui.prim) {
                return self.ui.prim.fadeIn();
              }
            });
          }
        };
        return self.alterna = setTimeout(changeLogo, 3000);
      }

      onBeforeDestroy() {
        return clearInterval(this.alterna);
      }

    };

    IntroItemView.prototype.template = '#intro-tela';

    IntroItemView.prototype.className = 'container';

    IntroItemView.prototype.ui = {
      prim: '.intro-primeira',
      seg: '.intro-segunda'
    };

    return IntroItemView;

  }).call(this);
  return IntroItemView;
});
