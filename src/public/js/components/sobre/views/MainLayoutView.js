// Generated by CoffeeScript 2.7.0
define(['marionette'], function(Marionette) {
  var MainLayoutView;
  MainLayoutView = (function() {
    class MainLayoutView extends Marionette.LayoutView {
      ['onRender']() {
        var self, slides, w;
        // console.log 'SobreMainViewOnRender'
        self = this;
        this.$el.find('[data-toggle="tooltip"]').tooltip();
        console.log(this.ui);
        this.ui.indicators.empty();
        slides = this.ui.inner.children();
        if (slides.length > 1) {
          slides.each(function(idx, el) {
            var indicator;
            indicator = $(`<li data-target='#carousel-example-generic' data-slide-to='${idx}'></li>`);
            indicator.css({
              'margin-left': 2,
              'margin-right': 2
            });
            if ($(el).hasClass('active')) {
              indicator.addClass('active');
            }
            return self.ui.indicators.append(indicator);
          });
        } else {
          this.ui.indicators.hide();
          this.ui.next.hide();
          this.ui.prev.hide();
        }
        window._tempLoh = setTimeout(function() {
          var altMenu;
          altMenu = $('.navbar-fixed-top').first().height();
          return $('.carousel-inner .item').css('min-height', $(window).height() / 2);
        }, 500);
        w = $(window);
        return w.on('resize', function() {
          clearTimeout(window._tempLoh);
          return window._tempLoh = setTimeout(function() {
            var altMenu;
            altMenu = $('.navbar-fixed-top').first().height();
            console.log(altMenu);
            $('mainStage').css('padding-top', altMenu + 10);
            return $('.carousel-inner .item').css('min-height', altMenu + 10);
          }, 500);
        });
      }

    };

    MainLayoutView.prototype['className'] = 'container-static sobre-main';

    MainLayoutView.prototype['template'] = '#sobre-main-layout';

    MainLayoutView.prototype['regions'] = {
      'contents': '#sobre-main-container'
    };

    MainLayoutView.prototype['ui'] = {
      'indicators': '.carousel-indicators',
      'inner': '.carousel-inner',
      'next': 'a.right',
      'prev': 'a.left'
    };

    return MainLayoutView;

  }).call(this);
  return MainLayoutView;
});
