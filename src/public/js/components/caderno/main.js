// Generated by CoffeeScript 2.7.0
define(['./routes', './controller'], function(routes, controller) {
  var cadernoModule;
  cadernoModule = function(component, parentApp, Backbone, Marionette, $, _) {
    var doItAll, key, match, prefixedRoutes, val;
    prefixedRoutes = {};
    for (key in routes) {
      val = routes[key];
      match = `comp/${this.moduleName}${key}`;
      console.log(match);
      prefixedRoutes[match] = val;
    }
    console.log(controller, prefixedRoutes);
    parentApp.appRouter.processAppRoutes(controller, prefixedRoutes);
    doItAll = function() {
      console.log(`${component.moduleName} Loaded!`);
      console.log('arguments', arguments);
      return console.log('context', this);
    };
    component.on("before:start", function() {
      return console.log('before:start', arguments);
    });
    component.on("start", function() {
      return console.log('start', arguments);
    });
    this.addInitializer(doItAll);
    component.addFinalizer(doItAll);
    console.log(`O contexto (this) é o próprio componente (component)
@ is component = ${this === component}
e como temos component não precisamos guardar o contexto (self = @)`);
    // Backbone, Marionette, $, _ de brinde no contexto, mano
    return console.log(component);
  };
  return cadernoModule;
});
