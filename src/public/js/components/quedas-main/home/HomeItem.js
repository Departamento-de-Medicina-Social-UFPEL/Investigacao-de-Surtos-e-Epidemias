// Generated by CoffeeScript 2.7.0
define([], function() {
  var QuedaItemView;
  QuedaItemView = (function() {
    class QuedaItemView extends Marionette.ItemView {
      initialize() {
        return console.log(arguments, this);
      }

      onRender() {
        var folder, urlFundo;
        folder = this.model.get('folder');
        urlFundo = `url(img/${folder}/${folder}_fundo.png)`;
        return this.$el.css({
          'background-image': urlFundo
        });
      }

      sayMe() {
        return App.appRouter.navigate(`comp/quedas-main/${this.model.get('folder')}`, {
          trigger: true
        });
      }

    };

    QuedaItemView.prototype.tagName = 'a';

    QuedaItemView.prototype.className = 'queda-item col-md-4 col-sm-6 col-xs-12';

    QuedaItemView.prototype.template = '#quedas-main-home-item';

    QuedaItemView.prototype.events = {
      'click': 'sayMe'
    };

    return QuedaItemView;

  }).call(this);
  return QuedaItemView;
});
