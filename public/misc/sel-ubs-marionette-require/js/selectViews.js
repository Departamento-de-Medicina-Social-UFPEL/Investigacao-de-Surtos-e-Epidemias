// Generated by CoffeeScript 1.9.0
(function() {
  var __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __hasProp = {}.hasOwnProperty;

  define([], function(SelectItemView) {
    var Estado, Municipio, Ubs;
    SelectItemView = (function(_super) {
      __extends(SelectItemView, _super);

      function SelectItemView() {
        return SelectItemView.__super__.constructor.apply(this, arguments);
      }

      SelectItemView.prototype.tagName = 'select';

      SelectItemView.prototype.className = 'form-control input-lg';

      SelectItemView.prototype.template = '#select-template';

      return SelectItemView;

    })(Marionette.ItemView);
    Estado = (function(_super) {
      __extends(Estado, _super);

      function Estado() {
        return Estado.__super__.constructor.apply(this, arguments);
      }

      Estado.prototype.events = {
        'change': 'mostra'
      };

      Estado.prototype.mostra = function() {
        return this.triggerMethod('mostra:municipiosDeUf', this.$el.val());
      };

      return Estado;

    })(SelectItemView);
    Municipio = (function(_super) {
      __extends(Municipio, _super);

      function Municipio() {
        return Municipio.__super__.constructor.apply(this, arguments);
      }

      Municipio.prototype.events = {
        'change': 'mostra'
      };

      Municipio.prototype.initialize = function() {
        return this.listenTo(this.collection, 'reset', this.render);
      };

      Municipio.prototype.mostra = function() {
        return this.triggerMethod('mostra:ubsDeMun', this.$el.val());
      };

      Municipio.prototype.onBeforeRender = function() {
        return this.$el.empty().attr({
          disabled: 'disabled'
        });
      };

      Municipio.prototype.onRender = function() {
        if (!(this.collection.length <= 1)) {
          return this.$el.removeAttr('disabled');
        }
      };

      return Municipio;

    })(SelectItemView);
    Ubs = (function(_super) {
      __extends(Ubs, _super);

      function Ubs() {
        return Ubs.__super__.constructor.apply(this, arguments);
      }

      Ubs.prototype.events = {
        'change': 'mostra'
      };

      Ubs.prototype.initialize = function() {
        return this.listenTo(this.collection, 'reset', this.render);
      };

      Ubs.prototype.mostra = function() {
        return this.triggerMethod('mostra:ubs', this.$el.val());
      };

      Ubs.prototype.onBeforeRender = function() {
        return this.$el.empty().attr({
          disabled: 'disabled'
        });
      };

      Ubs.prototype.onRender = function() {
        if (!(this.collection.length <= 1)) {
          return this.$el.removeAttr('disabled');
        }
      };

      return Ubs;

    })(SelectItemView);
    return {
      Estado: Estado,
      Municipio: Municipio,
      Ubs: Ubs
    };
  });

}).call(this);
