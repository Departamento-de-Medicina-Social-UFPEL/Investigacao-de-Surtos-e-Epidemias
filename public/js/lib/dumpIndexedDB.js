// Generated by CoffeeScript 2.7.0
define(['dexie'], function() {
  var dumpDB;
  console.log(arguments);
  dumpDB = function() {
    return Dexie.getDatabaseNames(function(databaseNames) {
      var dump;
      dump = function(databaseNames) {
        var db;
        if (databaseNames.length > 0) {
          db = new Dexie(databaseNames[0]);
          db.open().then(function() {
            db.delete();
          }).finally(function() {
            db.close();
            dump(databaseNames.slice(1));
          });
        } else {
          console.log(`     DBs Excluídos
==========================`);
        }
      };
      if (databaseNames.length === 0) {
        console.log("There are no databases at current origin. Try loading another sample and then go back to this page.");
      } else {
        dump(databaseNames);
      }
    });
  };
  return dumpDB;
});
