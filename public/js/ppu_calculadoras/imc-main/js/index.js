
$(document).ready(function(){
    // unasus.pack.inialize();
    // Generated by CoffeeScript 1.12.7
    requirejs.config({
        'baseUrl': 'js/',
        'waitSeconds': 50,
        'paths': {
        'jquery': 'jquery.min',
        'underscore': 'lodash.min',
        'backbone': 'backbone-min',
        'backbone.marionette': 'backbone.marionette.min',
        'bootstrap': 'bootstrap.min',
        'ripples': 'ripples.min',
        'material': 'material.min',
        'casca': 'imc-main'
            },
        'map': {
        '*': {
            'marionette': 'backbone.marionette'
        }
        },
        'shim': {
        'bootstrap': {
            'deps': ['jquery']
        },
        'material': {
            'deps': ['bootstrap']
        },
        'ripples': {
            'deps': ['bootstrap', 'material']
        },
        'casca': {
            'deps': ['backbone.marionette', 'material', 'ripples']
        }
        }
    });
    
    require(['casca'], function(CascaApp) {
        var goInit, status;
        
        window.App = new CascaApp();
        unasus.pack.inicializar();
        if (!unasus.pack.getStatus()) {
          status = {
            'status': 'attended',
            'percentage': 0,
            'LTIvalue': 0
          };
          unasus.pack.setStatus(status);
        }
        $('.main').append( window.App.render().el);
        // goInit = function() {
        //   };
        //   return goInit();
        
      });
  
    // $('.main').append(new ImcMainView());
})
