// Generated by CoffeeScript 1.8.0
define(['./lib/jsonStore', './controller', './lib/demo', './routes'], function(staticData, controller, Demografia, routes) {
  var UbsModule;
  UbsModule = function(compoment, parentApp, Backbone, Marionette, $, _) {
    var key, match, prefixedRoutes, val;
    prefixedRoutes = {};
    for (key in routes) {
      val = routes[key];
      match = "comp/" + this.moduleName + key;
      prefixedRoutes[match] = val;
    }
    parentApp.appRouter.processAppRoutes(controller, prefixedRoutes);
    parentApp.estados = staticData.estados;
    return parentApp.demo = new Demografia(staticData.faixaPorUf, staticData.faixaPorRelSexo);
  };
  return UbsModule;
});
