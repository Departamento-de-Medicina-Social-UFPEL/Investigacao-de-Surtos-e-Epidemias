// Generated by CoffeeScript 1.8.0
var __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define([], function() {
  var AdmissaoApresentacao;
  return AdmissaoApresentacao = (function(_super) {
    __extends(AdmissaoApresentacao, _super);

    function AdmissaoApresentacao() {
      return AdmissaoApresentacao.__super__.constructor.apply(this, arguments);
    }

    AdmissaoApresentacao.prototype.model = new Backbone.Model;

    AdmissaoApresentacao.prototype.fitScreen = function() {
      var fitView, h;
      fitView = this.ui.innerContainer;
      h = $(window).innerHeight() - 145;
      console.log(h);
      if (fitView) {
        return fitView.css({
          'minHeight': "" + h + "px"
        });
      }
    };

    AdmissaoApresentacao.prototype.topSetas = function() {
      var fitView, h, hf;
      fitView = this.ui.setas;
      h = this.ui.innerContainer.height();
      hf = ($(fitView[0]).height() / 2) - (h / 2);
      console.log(hf);
      if (fitView) {
        return fitView.css({
          'top': "" + hf + "px"
        });
      }
    };

    AdmissaoApresentacao.prototype.paddingTopCont = function(item) {
      var fitView, hf;
      fitView = this.ui.innerContainer;
      hf = item.height() / 2;
      console.log(hf);
      if (fitView) {
        return fitView.css({
          'padding-top': "" + hf + "px"
        });
      }
    };

    AdmissaoApresentacao.prototype.template = '#admissao-monitoramento-apresentacao';

    AdmissaoApresentacao.prototype['ui'] = {
      'carouselAdmissao': '#carousel-adimissao',
      'innerContainer': '.carousel-inner',
      'setas': '.carousel-control',
      'setaProx': '.carousel-control.right',
      'setaAnte': '.carousel-control.left'
    };

    AdmissaoApresentacao.prototype.onRender = function() {
      var fit, self;
      self = this;
      this.ui.carouselAdmissao.carousel('pause');
      this.ui.carouselAdmissao.swiperight(function() {
        return self.ui.carouselAdmissao.carousel('prev');
      });
      this.ui.carouselAdmissao.swipeleft(function() {
        return self.ui.carouselAdmissao.carousel('next');
      });
      fit = function() {
        return self.fitScreen;
      };
      $(window).on('resize', fit);
      return fit();
    };

    return AdmissaoApresentacao;

  })(Marionette.ItemView);
});
