// Generated by CoffeeScript 1.8.0
var makeIdade,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

define([], function() {
  var Paciente;
  Paciente = (function(_super) {
    __extends(Paciente, _super);

    function Paciente() {
      return Paciente.__super__.constructor.apply(this, arguments);
    }

    Paciente.prototype.template = '#caso-progresso-slides-paciente';

    Paciente.prototype.className = 'slide pacienteInfo slidePaciente container-fluid';

    Paciente.prototype.tagName = 'section';

    Paciente.prototype.initialize = function(options) {
      return console.log(this.model.get('dataLongaPub'));
    };

    Paciente.prototype['templateHelpers'] = {
      'strIdade': function() {
        return makeIdade(this.idade);
      }
    };

    Paciente.prototype.onRender = function() {
      return this.$el.attr('id', 'slidePaciente');
    };

    return Paciente;

  })(Marionette.ItemView);
  return Paciente;
});

makeIdade = function(strIdade) {
  var i, nomes, num, plural, ret, singular, uni;
  if (typeof strIdade !== "string") {
    return 0;
  }
  num = strIdade.match(/\d{1,}/g);
  uni = strIdade.match(/[dams]/g);
  singular = {
    d: "dia",
    a: "ano",
    m: "mês",
    s: "semana"
  };
  plural = {
    d: "dias",
    a: "anos",
    m: "meses",
    s: "semanas"
  };
  ret = "";
  for (i in num) {
    nomes = (parseInt(num[i], 10) > 1 ? plural : singular);
    ret += (ret.length > 0 ? " e " : "") + num[i] + " " + nomes[uni[i]];
  }
  return ret;
};
