// Generated by CoffeeScript 2.7.0
define([], function() {
  var Paciente;
  Paciente = class Paciente extends Backbone.Model {
    ['initialize'](options) {
      var fe, idade, sexo, somaPartes;
      if (typeof options !== 'object') {
        return;
      }
      idade = parseInt(options.idade.match(/\d{1,}/)[0]);
      sexo = options.sexo.trim();
      idade = options.idade.trim();
      if ((options.idade.indexOf('a') === -1) && (options.idade.indexOf('m') !== -1)) {
        idade = 1;
      }
      if ((options.idade.indexOf('a') !== -1) && (options.idade.indexOf('m') !== -1)) {
        somaPartes = function(soma, parte, idx) {
          var val;
          val = parseInt(parte.match(/\d{1,}/)[0]);
          if (idx === 0) {
            return soma + val;
          }
          return soma + (val / 12);
        };
        idade = idade.split('a').reduce(somaPartes, 0);
      }
      fe = (function() {
        switch (sexo) {
          case 'feminino':
            return 'fenoMulher';
          case 'masculino':
            return 'fenoHomem';
        }
      })();
      fe = (function() {
        switch (false) {
          case !(idade <= 13):
            return 'fenoCrianca';
          case !(idade <= 3):
            return 'fenoBebe';
          default:
            return fe;
        }
      })();
      console.log(`




`);
      console.log(options, fe, idade);
      console.log(`





`);
      return this.set('fenotipo', fe);
    }

  };
  return Paciente;
});
