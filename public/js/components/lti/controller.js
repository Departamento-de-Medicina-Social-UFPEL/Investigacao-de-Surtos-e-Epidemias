// Generated by CoffeeScript 1.8.0
define(['backbone.modal', 'marionette.modals'], function(ModalView, ModalsRegion, Template) {
  return {
    "describeInstance": function() {
      var Modal;
      Modal = Backbone.Modal.extend({
        template: _.template($('#lti-modal-info').html()),
        submitEl: '.bbm-button',
        onRender: function() {
          var b;
          b = $('body');
          if (!b.is(':visible')) {
            return b.fadeIn();
          }
        },
        onDestroy: function() {
          if (App.main.currentView) {
            return window.history.back();
          }
          return App.appRouter.navigate('/', {
            'trigger': true
          });
        }
      });
      return App.modals.show(new Modal());
    },
    "backToReturnUrl": function() {
      return window.location.href = window.lti.launch_presentation_return_url;
    }
  };
});
