// Generated by CoffeeScript 2.7.0
define(['./ItemView', '../collections/Monitoramentos'], function(ItemView, MonitoramentosColl) {
  var HomeLayout;
  HomeLayout = (function() {
    class HomeLayout extends Marionette.CompositeView {
      initialize() {
        var local, user;
        return ({user, local} = App);
      }

      onRender() {
        var self;
        self = this;
        //console.log @collection, 'collection'
        if (this.collection.length === 0) {
          return this.ui.locais.append('<li class="list-group-item"><h2><small>Você ainda não tem locais a serem exibidos. Clique em "Inserir Local"</small></h2></li>');
        }
      }

    };

    HomeLayout.prototype.childView = ItemView;

    HomeLayout.prototype.childViewContainer = '.list-group';

    //list = if local.get("monitoramento-#{user.cpf}") then local.get("monitoramento-#{user.cpf}") else [] 
    //App.monitoramentos = @collection
    //@collection = App.monitoramentos
    //console.log @collection.at(0), 'col1'
    HomeLayout.prototype.className = ' container';

    HomeLayout.prototype.template = '#monitoramento-covid-home';

    HomeLayout.prototype.ui = {
      'locais': '.list-group'
    };

    HomeLayout.prototype.events = {};

    return HomeLayout;

  }).call(this);
  return HomeLayout;
});
